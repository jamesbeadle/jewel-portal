diff --git a/.claude/skills/jpms-operator/references/assistant-and-data.md b/.claude/skills/jpms-operator/references/assistant-and-data.md
index 7e3500c..8596acc 100644
--- a/.claude/skills/jpms-operator/references/assistant-and-data.md
+++ b/.claude/skills/jpms-operator/references/assistant-and-data.md
@@ -37,7 +37,9 @@ Reads mirror the pages: `get_current_context`, `list_projects`,
 `get_request_context` / `list_request_correspondence` /
 `list_rfis_across_projects`, `list_variations` / `get_variation_context`,
 `list_bid_packages` / `get_bid_package_context` (every list carries the id its
-actions take), `list_work_orders` / `get_work_order_context`, `list_todos` /
+actions take), `list_work_orders` / `get_work_order_context` /
+`get_project_supplier_account` (one supplier's orders, invoices received and
+over-invoice on a project), `list_todos` /
 `get_todo_brief`, `list_defects`, `list_documents` / `get_document_extraction`
 / `list_document_triage`, `get_programme`, `list_progress`,
 `get_building_control`, `list_architect_instructions`, `list_calendar_events`,
diff --git a/.claude/skills/jpms-operator/references/site-map.md b/.claude/skills/jpms-operator/references/site-map.md
index e36fab6..065dc6c 100644
--- a/.claude/skills/jpms-operator/references/site-map.md
+++ b/.claude/skills/jpms-operator/references/site-map.md
@@ -216,6 +216,12 @@ drafts await two-click **Approve** (mints the next WO number and emails the PO
 from the projects mailbox) or Reject (terminal). Issued lines offer PO
 (printable page), Re-code (move/split across cost centres without changing
 value) and Cancel (MD/FD only; refused while bills linked or money paid).
+**Account** on a supplier row (supplier view) or **Supplier account…** in any
+order's Actions menu opens that supplier's account on the project: their orders
+with lines, invoiced and linked, paid, left to invoice; every invoice received
+(awaiting approval included) with its CIS labour / materials split, Xero status
+and the order(s) it is matched to; the over-invoice, if any; Download PDF to send
+on. Connector: `get_project_supplier_account`.
 Awarding a tender happens on the bid package; invoice linking on WO Allocation.
 
 ### Purchase order — `/projects/{project}/work-orders/{id}/po`
