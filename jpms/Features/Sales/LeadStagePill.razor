@* The stage pill every lead list and page shows — one look for one ladder: a Pill wrapper, the
   stage → tone mapping lives in StatusTones. *@
<Pill Tone="@Stage.ToTone()" Title="@Stage.Meaning()">@Stage.DisplayName()</Pill>

@code {
    [Parameter, EditorRequired] public LeadStage Stage { get; set; }
}
