namespace Jewel.JPMS.Features.Triage.Queue;

/// <summary>Which list the inbox pane shows: the live queue, the discarded pile or the tagged browser.</summary>
public enum QueueView { Active, Discarded, Tagged }
