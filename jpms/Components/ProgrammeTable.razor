<div class="rounded-xl border border-line bg-surface overflow-hidden">
    <table class="w-full text-sm">
        <thead class="bg-surface-raised border-b border-line">
            <tr class="text-left text-[11px] uppercase tracking-wider text-content-subtle font-semibold">
                <th class="px-4 py-3">Task</th>
                <th class="px-4 py-3 hidden sm:table-cell">Start</th>
                <th class="px-4 py-3 hidden sm:table-cell">End</th>
                <th class="px-4 py-3">Progress</th>
                <th class="px-4 py-3 hidden md:table-cell">BoQ link</th>
            </tr>
        </thead>
        <tbody class="divide-y divide-line">
            @foreach (var task in Tasks)
            {
                <tr>
                    <td class="px-4 py-3 text-content font-medium">@task.Title</td>
                    <td class="px-4 py-3 text-content-muted hidden sm:table-cell">@task.PlannedStart.LocalDateTime.ToString("d MMM")</td>
                    <td class="px-4 py-3 text-content-muted hidden sm:table-cell">@task.PlannedEnd.LocalDateTime.ToString("d MMM")</td>
                    <td class="px-4 py-3">
                        <div class="flex items-center gap-2 w-32">
                            <div class="flex-1 h-1.5 bg-surface-raised rounded-full overflow-hidden">
                                <div class="h-full bg-emerald-500" style="width: @((int)task.ProgressPercent)%"></div>
                            </div>
                            <span class="text-xs text-content-muted font-medium">@task.ProgressPercent.ToString("0")%</span>
                        </div>
                    </td>
                    <td class="px-4 py-3 hidden md:table-cell">
                        @if (!string.IsNullOrEmpty(task.BoqLineItemId))
                        {
                            <span class="font-mono text-xs text-content-subtle">@task.BoqLineItemId</span>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    [Parameter, EditorRequired] public IReadOnlyList<ProgrammeTask> Tasks { get; set; } = Array.Empty<ProgrammeTask>();
}
