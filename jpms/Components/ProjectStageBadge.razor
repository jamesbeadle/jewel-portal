<span class="inline-flex items-center gap-1.5 rounded-full bg-surface-raised border border-line px-2 py-0.5">
    <span class="inline-block w-1.5 h-1.5 rounded-full @Stage.AccentDotClass()"></span>
    <span class="text-[11px] font-medium text-content-muted">@Stage.DisplayName()</span>
</span>

@code {
    [Parameter, EditorRequired] public ProjectStage Stage { get; set; }
}
