@page "/projects/{ProjectId}/commercial"
@inject AuthService Auth
@inject SessionService Session
@inject ICommercialStore CommercialStore
@inject NavigationManager Nav
@implements IDisposable

<PageTitle>Commercial · JPMS</PageTitle>

@if (!isLoaded)
{
    <section class="max-w-5xl mx-auto px-4 py-12 text-slate-500 text-sm">Loading…</section>
}
else if (!Session.IsApproved)
{
    <RequestAccessView Email="@Auth.CurrentUser!.Email" DisplayName="@Auth.CurrentUser.DisplayName" />
}
else
{
    <ProjectPageShell ProjectId="@ProjectId" ActiveTab="commercial">
        <div class="space-y-6">
            <CvrSummaryPanel Snapshot="CommercialStore.LatestCvr(ProjectId)" />

            <section>
                <h2 class="text-lg font-semibold text-slate-900 mb-3">Programme Valuation Reports</h2>
                <ValuationTable Valuations="CommercialStore.ValuationsFor(ProjectId)" />
            </section>

            <section>
                <h2 class="text-lg font-semibold text-slate-900 mb-3">Cost code budgets</h2>
                <CostCodeBudgetTable Budgets="CommercialStore.BudgetsFor(ProjectId)" />
            </section>
        </div>
    </ProjectPageShell>
}

@code {
    [Parameter] public string ProjectId { get; set; } = "";

    private bool isLoaded;

    protected override async Task OnInitializedAsync()
    {
        await Session.EnsureLoadedAsync();
        if (!Auth.IsSignedIn) { Nav.NavigateTo("/login", forceLoad: true); return; }
        CommercialStore.OnChange += StateHasChanged;
        isLoaded = true;
    }

    public void Dispose() => CommercialStore.OnChange -= StateHasChanged;
}
