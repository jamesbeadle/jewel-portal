@page "/projects/{ProjectId}/operations-setup"
@inject NavigationManager Nav

@* The per-section Setup tabs are recombined into the single Project settings page. *@

@code {
    [Parameter] public string ProjectId { get; set; } = "";

    protected override void OnInitialized() =>
        Nav.NavigateTo($"/projects/{ProjectId}/settings", replace: true);
}
