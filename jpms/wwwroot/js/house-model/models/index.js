// Every model the viewer can mount, by the key the page names. One for now — the demo house —
// and a model per estimate joins here once the modelling route is agreed.
import { ravensDene16 } from "./ravens-dene-16.js";

export const modelsByKey = {
    [ravensDene16.key]: ravensDene16
};
