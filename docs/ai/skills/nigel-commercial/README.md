# Nigel's commercial skill pack — source copies

Extracted from the 2026-08-10 email "COMMERCIAL HEAD BRAIN WITH QS" so the pack lives in the repo
rather than only in an inbox. These are the SOURCE files; the live copies are in the portal's
skill store (Admin → AI Skills), where Nigel maintains them. When the two disagree, the portal wins.

To load: open Admin → AI Skills → New skill, paste a file whole (the ---frontmatter--- block fills
the key/description on paste), attach it to the `commercial` agent, and add the five files under
`references/` as reference documents of `nigel-commercial-doctrine`.

Two generations are here deliberately (`commercial-director` is the newer, business-agnostic build;
`nigel-commercial-doctrine` the Abbot Road original). Ingest both, mark the newer pinned, and
reconcile with Nigel once they have been used in anger — docs/ai/05-agents-and-skills.md §3.
