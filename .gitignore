# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# .NET / Blazor (for when we scaffold under /prototypes)
bin/
obj/
*.user
*.suo
.vs/

# Node (for any tooling we add later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Azure Functions local settings — may contain the Graph client secret. Never commit.
local.settings.json
**/local.settings.json
!**/local.settings.json.example
infra/.azure-output.env
infra/.azure-prod-output.env

# Master-admin seed (contains the password hash + plaintext login, references prod secrets)
api/Migrations/seed-master-admin.sql
infra/seed-master-admin.sh

# Build artefacts
publish/
jpms/wwwroot/css/app.built.css

# Logs / temp
*.log
*.tmp
.cache/

# Subcontractor / company directory seed (third-party contact data — load via sqlcmd, never commit)
scripts/seed-subcontractors.sql

# Local scratch area for files the Claude bridge cannot delete
_to_delete/

# Claude build-staging artifacts (safe to delete)
_nuget/
_nuget.zip
_stage_repo.zip
_stage_src.zip
_stage_src_*.zip
_copy-nuget-cache.sh
zi1qoCk6
zi84tD84
ziB7Ad8l
ziB7Ad8I

# Python bytecode from the refactor audit
__pycache__/
*.pyc
.claude-transfer/
Claude outputs/
